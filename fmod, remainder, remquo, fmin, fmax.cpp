#include <bits/stdc++.h>
using namespace std;
#define show(x) cout << #x << " : " << x << endl

int main ()
{
    double a, b, rem, rem2, rem3, mn, mx;
    int quo;
    cin >> a >> b; /// take input as a > b
    ///rem = fmod(a, b);/// the quotient computed is truncated
    ///cout << rem << endl; ///prints a - (int)(b * (a/b))
    ///rem2 = remainder(a, b); /// the quotient computed is rounded
    ///cout << rem2 << endl; /// (ceil(a/b) * b) - a;
    ///rem3 = remquo(a, b, &quo);
    ///cout << rem3 << ' ' << quo << endl;
    ///mn = fmin(a, b), mx = fmax(a, b);
    ///show(mn); show(mx);
}
